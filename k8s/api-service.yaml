apiVersion: v1
kind: Service
metadata:
  name: pente-api
  labels:
    app: pente-api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: pente-api
